# Watt Homebrew tap

Install [Watt](https://github.com/josipmusa/watt) with:

```sh
brew install --cask josipmusa/tap/watt
```

The Watt release workflow updates this tap automatically when a new semantic-version tag is published.
